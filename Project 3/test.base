logical a.
logical b.
integer c.
void d.

tuple yesui {
    integer mk.
    logical mt.
    void    am.
}.

logical x {integer y, integer z} [
    while (y < 2) [
        y = y + 2.
        z--.
    ]

    if (7 < z & 3 > z) [ write << z. ] else [ write << z+4.]

    return x(z, y).
]